//Filosofia de los modulos
//Los elementos del dom los voy a guardar y exportar

const ENTER_KEY = 13,
    c = console.log,
    d = document,
    j = json,
    ls = localStorage

// Destructuración
    export{
        ENTER_KEY,
        c,
        d,
        j,
        ls
    }